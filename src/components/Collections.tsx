import SearchButton from "./SearchButton";

const Collections = () => {
  return (
    <div>
      <h2 className="mb-2">🔍 Collections</h2>
      <p className="lead text-muted">
        Search and explore characters from the wizarding world.
      </p>
      <SearchButton />
    </div>
  );
};

export default Collections;
